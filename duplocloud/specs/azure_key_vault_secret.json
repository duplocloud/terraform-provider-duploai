{
  "name": "azure_key_vault_secret",
  "description": "Manages a secret inside an Azure Key Vault. Key Vault secrets are append-only: writing a name that already exists adds a new version rather than overwriting the old one, so any change here produces a new version and a new `version` value. The secret's value is write-only — the API never returns it, only its metadata.",
  "idPath": "name",
  "dataSource": true,
  "endpoint": {
    "uriBase": "/v1/aiservicedesk/user/data/workspaces/{workspace_id}/environment/AzureKeyVaults/{key_vault_id}/secrets"
  },
  "attributes": [
    {
      "name": "workspace_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "description": "Workspace that owns the vault (path parameter)."
    },
    {
      "name": "key_vault_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "description": "Backend id of the Key Vault holding this secret — the `vault_record_id` of a duploai_azure_key_vault, not the ARM resource id (path parameter)."
    },
    {
      "name": "name",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "name",
      "description": "Secret name, unique within the vault. Letters, numbers and hyphens only. Changing it creates a different secret, so the old one is destroyed — and Azure keeps a deleted name reserved in a recoverable state until it is purged or recovered."
    },
    {
      "name": "value",
      "type": "string",
      "required": true,
      "sensitive": true,
      "preserveOnEmptyResponse": true,
      "apiPath": "value",
      "description": "Secret value. Required — the API rejects a create or a new version without one. Write-only: reads return metadata only, so the configured value is kept in Terraform state and a change made outside Terraform cannot be detected. Because Key Vault is append-only, sending a value creates a new version even when it is unchanged."
    },

    {
      "name": "content_type",
      "type": "string",
      "optional": true,
      "computed": true,
      "apiPath": "contentType",
      "description": "Free-form hint describing what the value holds, e.g. \"text/plain\" or \"application/json\". Azure does not interpret it."
    },
    {
      "name": "enabled",
      "type": "bool",
      "optional": true,
      "computed": true,
      "apiPath": "enabled",
      "description": "Whether the secret can be read. Disabling keeps the secret and its versions but makes reads fail. Defaults to enabled."
    },
    {
      "name": "expires_on",
      "type": "string",
      "optional": true,
      "computed": true,
      "apiPath": "expiresOn",
      "description": "When the secret stops being valid, as an RFC 3339 timestamp. Azure does not delete an expired secret — reads simply fail. Leave unset for no expiry."
    },
    {
      "name": "not_before",
      "type": "string",
      "optional": true,
      "computed": true,
      "apiPath": "notBefore",
      "description": "When the secret becomes valid, as an RFC 3339 timestamp. Reads before this time fail. Leave unset to make it valid immediately."
    },
    {
      "name": "tags",
      "type": "map(string)",
      "optional": true,
      "computed": true,
      "apiPath": "tags",
      "description": "Free-form key/value tags on the secret."
    },

    {
      "name": "secret_id",
      "type": "string",
      "computed": true,
      "noSend": true,
      "apiPath": "id",
      "description": "Full Azure identifier of this secret version, e.g. https://my-vault.vault.azure.net/secrets/db-password/<version>."
    },
    {
      "name": "version",
      "type": "string",
      "computed": true,
      "noSend": true,
      "apiPath": "version",
      "description": "Identifier of the current version. Changes every time a value is written, including when the value is unchanged."
    },
    {
      "name": "created_on",
      "type": "string",
      "computed": true,
      "noSend": true,
      "apiPath": "createdOn",
      "description": "When this version was created (RFC 3339)."
    },
    {
      "name": "updated_on",
      "type": "string",
      "computed": true,
      "noSend": true,
      "apiPath": "updatedOn",
      "description": "When this version was last updated (RFC 3339)."
    }
  ]
}
