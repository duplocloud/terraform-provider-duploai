{
  "name": "plan",
  "description": "Manages a DuploCloud AI Helpdesk plan (region landing zone built on a network baseline).",
  "idPath": "id",
  "dataSource": true,
  "endpoint": {
    "uriBase": "/v1/aiservicedesk/user/data/workspaces/{workspace_id}/environment/Plans"
  },
  "attributes": [
    {
      "name": "workspace_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "description": "ID of the workspace that owns this plan."
    },
    {
      "name": "name",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "name",
      "description": "Name of the plan."
    },
    {
      "name": "description",
      "type": "string",
      "optional": true,
      "computed": true,
      "default": "",
      "apiPath": "description",
      "description": "Optional description."
    },
    {
      "name": "scope_ids",
      "type": "list(string)",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.scopeIds",
      "description": "Scope IDs that link this plan to a cloud provider account."
    },
    {
      "name": "region",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.region",
      "description": "AWS region (e.g. us-east-1)."
    },
    {
      "name": "network_baseline_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.networkBaselineId",
      "description": "ID of the network baseline this plan is built on."
    },
    {
      "name": "provisioner_type",
      "type": "string",
      "optional": true,
      "computed": true,
      "default": "Cli",
      "forceNew": true,
      "apiPath": "spec.provisioner.type",
      "description": "Provisioner type: Cli, IacNativeTf, IacDuploTf, or DirectApiCall."
    },
    {
      "name": "provisioner_version",
      "type": "string",
      "optional": true,
      "computed": true,
      "default": "",
      "forceNew": true,
      "apiPath": "spec.provisioner.version",
      "description": "Optional provisioner version."
    },
    {
      "name": "status",
      "type": "string",
      "computed": true,
      "apiPath": "status",
      "description": "Current provisioning status."
    },
    {
      "name": "plan_id",
      "type": "string",
      "computed": true,
      "stable": true,
      "apiPath": "id",
      "description": "ID of this plan, for reference by dependent resources."
    },
    {
      "name": "primary_hosted_zone_id",
      "type": "string",
      "optional": true,
      "computed": true,
      "apiPath": "result.primaryHostedZone.hostedZoneId",
      "description": "ID of the primary Route 53 hosted zone. Set to bring an existing hosted zone; leave unset to have the platform provision one."
    },
    {
      "name": "primary_hosted_zone_domain",
      "type": "string",
      "optional": true,
      "computed": true,
      "apiPath": "result.primaryHostedZone.domainName",
      "description": "Domain name of the primary hosted zone. Set to bring an existing domain; leave unset to have the platform provision one."
    },
    {
      "name": "certificates",
      "type": "list(object)",
      "optional": true,
      "computed": true,
      "apiPath": "result.certificates",
      "description": "ACM certificates for the plan. Set to bring existing certificates; leave unset to have the platform provision them.",
      "attributes": [
        {
          "name": "certificate_arn",
          "type": "string",
          "optional": true,
          "computed": true,
          "apiPath": "certificateArn",
          "description": "ARN of the ACM certificate."
        },
        {
          "name": "domain",
          "type": "string",
          "optional": true,
          "computed": true,
          "apiPath": "domainName",
          "description": "Domain the certificate covers."
        }
      ]
    },
    {
      "name": "azure_certificates",
      "type": "list(object)",
      "optional": true,
      "computed": true,
      "apiPath": "result.azureResult.azureCertificates",
      "description": "Azure Key Vault certificates registered with this plan, for the AGIC Application Gateway to serve. Each entry is a REFERENCE to a certificate that already exists in a Key Vault — the platform does not create or provision certificates, so leaving this unset simply means none are registered. On reconcile the platform reads the referenced secret itself and pushes the PFX into the Application Gateway. Names must be unique within the plan, compared case-insensitively; an entry whose name is already registered by another plan on the same cluster is ignored.",
      "attributes": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "apiPath": "name",
          "description": "Application Gateway SSL certificate name, used verbatim, so it must satisfy Azure's SSL-certificate naming convention: 1-80 characters, starting with a letter or digit and containing only letters, digits, `.`, `_` or `-`. Must be unique within the plan (case-insensitive). The API rejects anything else with HTTP 400."
        },
        {
          "name": "key_vault_secret_id",
          "type": "string",
          "required": true,
          "apiPath": "keyVaultSecretId",
          "description": "Key Vault secret URI holding the certificate's PFX, `https://<vault>.vault.azure.net/secrets/<name>` with an optional trailing `/<version>`. Prefer the UNVERSIONED form: the platform resolves the URI on every reconcile, so an unversioned URI picks up certificate rotations automatically while a versioned one pins that exact version forever. The API rejects a URI that is not `https://.../secrets/...` with HTTP 400."
        }
      ]
    },
    {
      "name": "ami_ids",
      "type": "list(string)",
      "computed": true,
      "apiPath": "result.amis[].amiId",
      "description": "IDs of AMIs registered by the plan."
    }
  ],
  "waiter": {}
}
