{
  "name": "azure_private_endpoint",
  "description": "Manages an Azure Private Endpoint, giving a PaaS resource a private IP address inside a subnet so it can be reached without traversing the public internet.\n\nThe platform does two things per endpoint: it creates the `Microsoft.Network/privateEndpoints` resource, then attaches a Private DNS zone group so the target's public hostname resolves to the private IP from inside the network. The zone is chosen from `sub_resource_name` — `blob` gives `privatelink.blob.core.windows.net`, `vault` gives `privatelink.vaultcore.azure.net`, and so on. The resource only reports Complete once both steps have succeeded, so a Complete endpoint always has working private DNS. Verified live 2026-08-05 against a storage account's blob service.\n\nEverything about an endpoint is immutable: Azure has no in-place update for one, and the API rejects the attempt outright. Any change here — including tags — replaces the endpoint.",
  "idPath": "id",
  "dataSource": true,
  "endpoint": {
    "uriBase": "/v1/aiservicedesk/user/data/workspaces/{workspace_id}/environment/AzurePrivateEndpoints",
    "immutable": true
  },
  "createConstants": [
    {
      "path": "spec.mode",
      "value": "Create"
    }
  ],
  "waiter": {
    "pollIntervalSeconds": 15,
    "createTimeoutMinutes": 30,
    "deleteTimeoutMinutes": 20
  },
  "attributes": [
    {
      "name": "workspace_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "description": "ID of the workspace that owns this private endpoint."
    },
    {
      "name": "name",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "name",
      "description": "Private endpoint name. Also names the private-link service connection inside it."
    },
    {
      "name": "environment_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.environmentId",
      "description": "ID of the environment in which the private endpoint is provisioned."
    },
    {
      "name": "resource_group_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.resourceGroupId",
      "description": "ID of the resource group in which the private endpoint is provisioned. Its Azure resource group is where the endpoint is created unless `azure_resource_group_name` overrides it."
    },
    {
      "name": "target_resource_arm_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.targetResourceArmId",
      "description": "Full Azure resource ID (ARM ID) of the PaaS resource the endpoint connects to. Other resources in this provider expose theirs: `duploai_storage_account.azure_resource_id`, `duploai_azure_key_vault.key_vault_id`, `duploai_azure_managed_redis.cluster_id`.\n\nThe target must already be provisioned — the API rejects the create otherwise, because Azure needs the resource to exist before a private-link connection can be made to it."
    },
    {
      "name": "sub_resource_name",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.subResourceName",
      "description": "Which part of the target resource the endpoint exposes — Azure calls it the private-link group ID, and it must be one the target resource type actually offers. One endpoint serves one sub-resource, so a storage account needing both blob and file access needs two endpoints.\n\nThe value also decides the Private DNS zone the platform attaches, and it knows these:\n\n| `sub_resource_name` | Target | DNS zone |\n|---|---|---|\n| `blob`, `file`, `queue`, `table`, `dfs`, `web` | Storage account | `privatelink.<name>.core.windows.net` |\n| `vault` | Key Vault | `privatelink.vaultcore.azure.net` |\n| `redisenterprise` | Azure Managed Redis | `privatelink.redis.azure.net` |\n\nAny other value is accepted, and the endpoint is still created with a working private IP — but the platform derives the zone name by pattern, `privatelink.<value>.core.windows.net`, which is wrong for every service that does not live under that suffix. A `sqlServer` endpoint needs `privatelink.database.windows.net` and would instead get a `privatelink.sqlserver.core.windows.net` zone that resolves nothing. For those services the endpoint works at the IP level and private DNS has to be arranged separately, so check `dns_zone_name` after creating one."
    },
    {
      "name": "subnet_id",
      "type": "string",
      "required": true,
      "forceNew": true,
      "apiPath": "spec.subnetId",
      "description": "Full Azure resource ID (ARM ID) of the subnet that holds the endpoint's network interface, available as `duploai_network_baseline.azure_subnet_ids`. The subnet must allow private endpoints, and each endpoint consumes one private IP address from it."
    },
    {
      "name": "azure_resource_group_name",
      "type": "string",
      "optional": true,
      "computed": true,
      "forceNew": true,
      "apiPath": "spec.azureResourceGroupName",
      "description": "Azure resource group the endpoint is created in. Defaults to the Azure resource group behind `resource_group_id`, which is almost always what you want — set it only to place the endpoint elsewhere."
    },
    {
      "name": "tags",
      "type": "map(string)",
      "optional": true,
      "computed": true,
      "forceNew": true,
      "filterResponseKeys": [
        "duplocloud-ai-*"
      ],
      "apiPath": "spec.tags",
      "description": "Tags applied to the private endpoint. The platform adds its own managed `duplocloud-ai-*` tags server-side; those are filtered out of state so only your tags are managed by Terraform.\n\nChanging a tag replaces the endpoint, since the API has no update path at all."
    },
    {
      "name": "provisioner_type",
      "type": "string",
      "optional": true,
      "computed": true,
      "forceNew": true,
      "oneOf": [
        "Cli",
        "IacNativeTf",
        "IacDuploTf",
        "DirectApiCall"
      ],
      "apiPath": "spec.provisioner.type",
      "description": "Provisioner type. Assigned by the platform when unset."
    },
    {
      "name": "provisioner_version",
      "type": "string",
      "optional": true,
      "computed": true,
      "forceNew": true,
      "apiPath": "spec.provisioner.version",
      "description": "Optional provisioner version. Left unset the API omits it, so this reads back empty rather than as an empty string."
    },
    {
      "name": "scope_ids",
      "type": "list(string)",
      "computed": true,
      "stable": true,
      "apiPath": "spec.scopeIds",
      "description": "Scope IDs linking this private endpoint to a cloud provider account, inherited from the resource group."
    },
    {
      "name": "private_endpoint_id",
      "type": "string",
      "computed": true,
      "stable": true,
      "apiPath": "id",
      "description": "ID of this private endpoint, for reference by dependent resources."
    },
    {
      "name": "unique_cloud_resource_id",
      "type": "string",
      "computed": true,
      "stable": true,
      "apiPath": "spec.uniqueCloudResourceId",
      "description": "Full Azure resource ID (ARM ID) of the private endpoint, as the platform records it on the resource itself. It is what the delete and refresh operations use to reach the endpoint, so prefer this one when referencing the endpoint's ARM ID. Normally identical to `private_endpoint_arm_id`; the two are separate because the platform reads one and falls back to the other, which can differ for an endpoint it discovered in Azure rather than created."
    },
    {
      "name": "private_endpoint_arm_id",
      "type": "string",
      "computed": true,
      "stable": true,
      "apiPath": "result.privateEndpointArmId",
      "description": "Full Azure resource ID (ARM ID) of the private endpoint, as reported in the creation result. Normally identical to `unique_cloud_resource_id` — prefer that one, which is the value the platform's own delete and refresh operations use."
    },
    {
      "name": "private_ip_address",
      "type": "string",
      "computed": true,
      "apiPath": "result.privateIpAddress",
      "description": "Private IP address assigned to the endpoint's network interface, and the address its DNS zone resolves the target's hostname to. Allocated from `subnet_id`'s range. Empty until Azure finishes provisioning, since it is read from the interface rather than chosen up front."
    },
    {
      "name": "provisioning_state",
      "type": "string",
      "computed": true,
      "apiPath": "result.provisioningState",
      "description": "Azure provisioning state of the endpoint, e.g. Succeeded or Creating, refreshed from Azure on each read."
    },
    {
      "name": "dns_zone_group_provisioned",
      "type": "bool",
      "computed": true,
      "apiPath": "result.dnsZoneGroupProvisioned",
      "description": "Whether the Private DNS zone group is attached. The endpoint only reaches Complete once this is true, so a Complete endpoint always resolves privately; while it is false the endpoint exists but the target's hostname still resolves publicly."
    },
    {
      "name": "dns_zone_name",
      "type": "string",
      "computed": true,
      "apiPath": "result.dnsZoneName",
      "description": "Private DNS zone attached to the endpoint, derived from `sub_resource_name` — e.g. `privatelink.blob.core.windows.net`."
    },
    {
      "name": "dns_zone_arm_id",
      "type": "string",
      "computed": true,
      "apiPath": "result.dnsZoneArmId",
      "description": "Full Azure resource ID (ARM ID) of the Private DNS zone created or reused for this endpoint. The zone lives in a platform-shared resource group per subscription (`duplo-shared-rg-<subscription-id>`), not in this endpoint's own resource group, and it is reused — every endpoint for the same sub-resource type across the subscription reports the same value, so deleting one endpoint does not remove the zone."
    },
    {
      "name": "status",
      "type": "string",
      "computed": true,
      "apiPath": "status",
      "description": "Current provisioning status of the private endpoint. Complete means the endpoint exists in Azure and its DNS zone group is attached."
    },
    {
      "name": "created_at",
      "type": "string",
      "computed": true,
      "stable": true,
      "apiPath": "createdAt",
      "description": "Timestamp when the private endpoint was created (RFC 3339)."
    }
  ]
}
